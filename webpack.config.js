const path = require('path');
const { loaders } = require('./config/webpack/loaders');

module.exports = {
  mode: 'development',
  entry: './src/index.js',
  output: {
    filename: 'main.js',
    path: path.resolve(__dirname, 'dist'),
    clean: true,
  },
  module: {
    rules: loaders(),
  }
}